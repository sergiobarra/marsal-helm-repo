apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "marsal-o5gs.fullname" . }}-test-connection"
  labels:
    {{- include "marsal-o5gs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "marsal-o5gs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
