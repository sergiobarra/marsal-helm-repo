apiVersion: v1
kind: Service
metadata:
  name: {{ include "marsal-o5gs.fullname" . }}
  labels:
    {{- include "marsal-o5gs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marsal-o5gs.selectorLabels" . | nindent 4 }}
